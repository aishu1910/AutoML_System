# AutoML_System
A user-friendly AutoML pipeline built with Streamlit and FastAPI that enables users to upload datasets, select target variables, and automatically train multiple machine learning models. The system evaluates performance, performs hyperparameter tuning, and returns the best model as a downloadable .pkl file for real-world use.
